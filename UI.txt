!!IMPORTANT!!

ONLY CREATE A UI FOR NOW, DON'T CREATE ANY LOGIC, FUNCTIONS

!!IMPORTANT!!


1. Dropdown menu with name "zoom_levels" so that I can for example call in code zoom_levels.value
With example input, easy to change.

2. Dropdown menu with name "file_type" so that I can for example call in code
file_type.value to change output file extension. With values: webp, png, easy to change.

3. Create zoom level component it will contain text input zoom_level_name and integer input with label
"square size" so only one number input required.

4. A button with name "generate_image" and text "Generate image", it will open file explorer 
and let me save photo with one of the earlier defined extensions webp or png


5. A button with name "generate_tiles" and text "Generate tiles", it will let me
generate folder with tile set using dzsave save them with one of the earlier 
defined extensions webp or png

6. Image viewer to which I can drag and drop only images with different extensions into it or click
onto it to open file explorer and choose image.


7. All stylings and layout are up to you but in case of wanting to change it I want to be able
to do it easily.

8. Add comments explaining which thing is for what, also create UI readme.